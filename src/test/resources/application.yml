spring:
  application:
    name: "demo"
  datasource:
    driverClassName: org.postgresql.Driver
    url: "jdbc:localhost:3456/test"
    username: "sa"
    password: ""
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: false